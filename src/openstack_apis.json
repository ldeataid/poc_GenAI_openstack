{
    "Functions": [
        {
            "path": "IDENTITY.services.list",
            "parameters": "",
            "action": "List services"
        },
        {
            "path": "IDENTITY.services.show",
            "parameters": "service_name",
            "action": "Show a specific service"
        }
    ]
}
